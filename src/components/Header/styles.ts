import styled from 'styled-components';

import bannerBackground from 'assets/marvel.jpg';

export const BannerContainer = styled.section`
  background-image: url(${bannerBackground});
`;

export const Title = styled.h1`
  color: white;
`;
